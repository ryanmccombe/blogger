<h1>All Tags</h1>
<ul id="tags">
  <% @tags.each do |tag| %>
      <li>
        <%= link_to tag.name, tag_path(tag), class: "tag_link", id:"tag_#{tag.id}" %>
        (<%= link_to "Delete", tag_path(tag), method: :delete, data: {confirm: "For realsies?"} %>)
      </li>
  <% end %>
</ul>